rules_version = '2';

// ⚠️⚠️⚠️ NO PUBLICAR EN F0 ⚠️⚠️⚠️
//
// Este archivo es DOCUMENTACIÓN de referencia para coordinar en F4
// (cuando GymChat migre a Firestore modular). Las reglas ACTIVAS
// viven en Firebase Console del proyecto fittrack-e06be y las
// comparte el app vieja — publicar esto a ciegas podría bloquear
// al FitTrack actual que sigue instalado.
//
// Borrador de referencia (colecciones detectadas en la auditoría):
//   gymchats/{codigo}/mensajes/{id}   — chat de gimnasio por código
//   fittrack_usuarios/{uid}           — perfil público mínimo
//   fittrack_sync/{uid}               — F8: respaldo completo del usuario
//
// service cloud.firestore {
//   match /databases/{database}/documents {
//     // Perfil propio
//     match /fittrack_usuarios/{uid} {
//       allow read, write: if request.auth != null && request.auth.uid == uid;
//     }
//     // Chat del gimnasio: cualquier usuario autenticado con el código
//     match /gymchats/{codigo}/mensajes/{mensajeId} {
//       allow read, create: if request.auth != null;
//     }
//     // F8 · Sync: TODO el progreso del usuario (solo él lee/escribe)
//     match /fittrack_sync/{uid} {
//       allow read, write: if request.auth != null && request.auth.uid == uid;
//     }
//   }
// }
